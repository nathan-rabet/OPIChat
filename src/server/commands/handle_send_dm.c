#include <stddef.h>

#include "client.h"
#include "commands.h"
#include "message.h"

struct message *handle_send_dm(struct message *msg, struct client *client)
{
    // TODO
    (void)msg;
    (void)client;
    return NULL;
}
